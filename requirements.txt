numpy==1.24.3          # TensorFlow와의 호환성을 위해 다운그레이드 필요
tensorflow==2.14.0     # 현재 설치된 버전 유지
pandas==2.2.3          # 현재 설치된 버전 유지
scikit-learn==1.6.0    # 현재 설치된 버전 유지
matplotlib==3.10.0     # 현재 설치된 버전 유지
scipy==1.14.1          # 현재 설치된 버전 유지
glob2==0.7             # 현재 설치된 버전 유지
protobuf==4.25.5       # 현재 설치된 버전 유지
seaborn==0.13.2        # 현재 설치된 버전 유지